<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".OptionActivity" >

    <TextView
        android:id="@+id/favoriteFoodTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="@string/dynamic_generate" />

    <Button
        android:id="@+id/optionList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/favoriteFoodTextView"
        android:layout_marginTop="159dp"
        android:layout_toLeftOf="@+id/favoriteFoodTextView"
        android:text="排泄清單" />

    <Button
        android:id="@+id/optionPetInfo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/optionList"
        android:layout_alignBottom="@+id/optionList"
        android:layout_toRightOf="@+id/favoriteFoodTextView"
        android:text="寵物資訊" />

</RelativeLayout>